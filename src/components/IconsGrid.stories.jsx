import React from "react";
import IconsGrid from "./IconsGrid";

export default {
  title: "Components/IconsGrid",
  component: IconsGrid,
};

const Template = () => <IconsGrid />;

export const Default = Template.bind({});
